# DuckDB local warehouse
*.duckdb
*.duckdb.wal

# DuckLake raw layer (parquet + metadata; rebuilt by ingestion)
palm_lake/

# dbt
target/
dbt_packages/
logs/
.user.yml

# Python
.venv/
__pycache__/
*.pyc

# secrets / env
.env
*.token
profiles.local.yml

# local session handoff (contains private career/security context — do not publish)
HANDOFF.md

# agent-skills install (restore with: npx skills experimental_install) — content not committed
.agents/
.claude/skills/

# ingestion provenance (generated per run; uploaded as artifact)
ingestion_manifest.json
